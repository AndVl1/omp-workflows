node_modules
dist
.DS_Store
*.log
.env
.env.local
.env.*.local
coverage
.nyc_output
.vscode
.idea

# Local runtime / workflow state — per-machine, never committed
.omp/
.work-state/

# UX E2E run evidence (heavy: transcripts, screenshots) — keep local, reports go to git
vibe-report/evidence/
