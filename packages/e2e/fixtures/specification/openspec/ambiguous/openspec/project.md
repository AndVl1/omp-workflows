# Project: Payments Platform

## Conventions

- Capabilities live under `openspec/specs/{capability}/spec.md`.
- Active change proposals live under `openspec/changes/{change-id}/`.
- Every delta names its baseline capability explicitly.
