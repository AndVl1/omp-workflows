{
  "name": "@omp-workflows/content",
  "version": "0.1.0",
  "description": "Source of truth for omp-workflows agent + skill markdown bundles. Internal — not published to npm; fullstack references these files via local symlinks so claude-plugin / future bundles can pull from one place.",
  "private": true,
  "type": "module",
  "license": "MIT",
  "files": [
    "agents",
    "skills"
  ],
  "scripts": {
    "test": "node -e \"console.log('@omp-workflows/content: ' + require('fs').readdirSync('agents').length + ' agents, ' + require('fs').readdirSync('skills').length + ' skills')\""
  },
  "engines": {
    "node": ">=20"
  }
}
